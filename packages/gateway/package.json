{
  "name": "@taintmcp/gateway",
  "private": true,
  "version": "0.1.0",
  "description": "taintmcp gateway: presents as an MCP server to the agent, connects as an MCP client to downstream server(s).",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "taintmcp": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "commander": "^13.1.0",
    "express": "^5.1.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.5",
    "typescript": "^5.9.3"
  }
}
