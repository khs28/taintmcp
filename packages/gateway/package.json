{
  "name": "@taintmcp/gateway",
  "private": true,
  "version": "0.1.0",
  "description": "taintmcp gateway: presents as an MCP server to the agent, an MCP client to the downstream server(s).",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "taintmcp": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "commander": "^13.1.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
